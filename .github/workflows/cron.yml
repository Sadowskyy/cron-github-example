on:
  push:
    branches:
      - master
  schedule:
    - cron: '*/15 * * * * '
jobs:
  google:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        run: |
          script.sh
        shell: bash
